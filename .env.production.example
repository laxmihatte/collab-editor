# Copy to .env.production on the server and fill in. Never commit the real one.

# The domain Caddy will request a certificate for. DNS must already point here.
DOMAIN=notecraft.com

# Generate both with: openssl rand -base64 36
POSTGRES_PASSWORD=
JWT_SECRET=
