# Server
PORT=3001
CLIENT_URL=http://localhost:3000

# PostgreSQL
DATABASE_URL=postgresql://localhost:5432/notecraft

# Secret for signing auth tokens — use any long random string.
# Generate one with: openssl rand -base64 48
JWT_SECRET=

# Self-hosted code execution sandbox (docker compose up -d)
PISTON_URL=http://localhost:2000/api/v2/execute
